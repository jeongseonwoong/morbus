 function searchVal() {
    const searchText= document.querySelector("#search")
     printf("fsdjfldf");
     if(searchText.length===0||searchText.length>10)
     {
         alert("증상을 다시 입력해주세요");
     }
}
